<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @manga.title %>
</p>

<p>
  <strong>Number of pages:</strong>
  <%= @manga.number_of_pages %>
</p>

<p>
  <strong>Price:</strong>
  <%= @manga.price %>
</p>

<p>
  <strong>Author:</strong>
  <%= @manga.author %>
</p>

<p>
  <strong>Pulisher:</strong>
  <%= @manga.pulisher %>
</p>
<p>

  <% if @manga.image.attached? %>

    <%= image_tag @manga.image %>

  <% end %>

</p>
<h2>Review From</h2>
<%= form_with(model: @manga_review, local: true) do |form| %>
  <div class= "field">
    <%= form.text_area :review %>
    <%= form.hidden_field :manga_id, :value => @manga.id %>
    <%= form.hidden_field :user_id, :value => current_user.id %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<h2>Review List</h2>
<ul>
  <% @manga.manga_review.each do |manga_review|%>
   <li><%= manga_review.review %>
     (<i><%= manga_review.user.name if manga_review.user %></i>)</li>
<% end %>
</ul>
<%= link_to 'Edit', edit_manga_path(@manga) %> |
<%= link_to 'Back', mangas_path %>


<p id="notice"><%= notice %></p>
<p>
  <strong>Title:</strong>
  <%= @manga.title %>
</p>
<p>
  <strong>Number of pages:</strong>
  <%= @manga.number_of_pages %>
</p>
<p>
  <strong>Detail:</strong>
  <%= @manga.detail %>
</p>
<p>
  <% if @manga.image.attached? %>
    <%= image_tag @manga.image %>
  <% end %>
</p>
<h2>Review From</h2>
<%= form_with(model: @manga_review, local: true) do |form| %>
  <div class= "field">
    <%= form.text_area :review %>
    <%= form.hidden_field :manga_id, :value => @manga.id %>
  </div>
  <div class="actions">
    <%= form.submit %>
<div class="container">
  <div class="row">
    <div class="col-sm-8">
      <div class="card mb-3">
        <h3 class="card-header">本詳細</h3>
        <div class="card-body">
          <h5 class="card-title"><%= @manga.title %></h5>
          <h6 class="card-subtitle text-muted">ページ数：<%= @manga.number_of_pages %></h6>
  　　　　</div>
  　　　　<% if @manga.image.attached? %>
          <%= image_tag @manga.image, class: "card-img" %>
        <% end %>
        <div class="card-body">
          <p class="card-text"><%= @manga.detail %></p>
        </div>
      </div>
    </div>
  <div class="col-sm-4">
    <div class="card bg-light mb-3" style="max-width: 40rem;">
      <div class="card-header">レビュー</div>
      <div class="card-body">
        <ul>
          <% @manga.book_review.each do |manga_review|%>
          　<div class="card border-light mb-3" style="max-width: 20rem;">
              <div class="card-header"><%= manga_review.user.name %></div>
              <div class="card-body">
                <p class="card-text"><%= manga_review.review %></p>
              </div>
            </div>
          <% end %>
        </ul>
      <%= form_with(model: @manga_review, local: true) do |form| %>
        <div class= "field">
          <%= form.text_area :review, :class => "form-control" %>
          <%= form.hidden_field :manga_id, :value => @manga.id %>
        </div>
        <div class="actions">
          <%= form.submit %>
        </div>
       <% end %>
       </div>
    </div>
  </div>
<% end %>
<h2>Review List</h2>
<ul>
  <% @manga.manga_review.each do |manga_review|%>
   <li><%= manga_review.review %>
     (<i><%= manga_review.user.name if manga_review.user%></i>)</li>
<% end %>
</ul>
</div>

<%= link_to 'Edit', edit_manga_path(@manga) %> |
<%= link_to 'Back', mangas_path %>